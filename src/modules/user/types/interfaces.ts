export interface ICategoryImage {
	path: string
	name: string
}
export interface IUser {
	id?: string
	name: string
	email: string
	mobile: string
	role: string
	isActive: boolean
	isVerified: boolean
}
